# 워커 및 스케줄러 아키텍처 상세 명세서 (Scheduler & Worker Specs)

본 문서는 Oracle Cloud Free Tier (1 OCPU, 1GB RAM) 환경의 극단적인 리소스 제약을 극복하고, KIS OpenAPI의 엄격한 호출 제한(Rate Limit)을 우회하기 위한 백엔드 작업 파이프라인의 핵심 구조를 설명합니다.

---

## 1. SystemScheduler (중앙 통제 스케줄러)

메모리 오버헤드와 이벤트 루프 충돌을 막기 위해 모든 예약된 작업(Cron Jobs)은 단일 `SystemScheduler` (Singleton 패턴 기반 `AsyncIOScheduler`) 하에서 중앙 통제됩니다.

### 구동 주기 및 타임라인 (Timeline)
- **`08:30` (마스터 갱신)**: 장 시작 30분 전, 거래 정지나 단기 과열에 진입한 위험 종목을 걷어내기 위해 `stock_codes` 테이블을 전면 초기화(`init_stock_codes_db`) 합니다.
- **`22:00` (인증 토큰 갱신)**: 다음 날 원활한 장중 API 통신을 위해 KIS Auth Token을 갱신합니다. (서버 부팅 시에도 1회 즉시 강제 실행됩니다).
- **`16:00` (일봉/수급 적재 - 예정)**: 장이 완전히 마감된 후, 전 종목에 대해 당일 일봉(`daily_ohlcv`) 및 수급 데이터(`daily_investors`)를 다운로드하여 핫리스트를 갱신합니다.

---

## 2. 서버 부트스트랩 파이프라인 (Bootstrap Pipeline)

서버 최초 구동 시 DB가 텅 비어있거나, 장기간 서버가 꺼져 있어 대규모 데이터 적재가 필요할 때 FastAPI 응답을 블로킹하지 않도록 설계된 백그라운드 초기화 자동화 시스템입니다.
- **비동기 Non-blocking**: `main.py`의 `lifespan` 훅 안에서 `asyncio.create_task`로 던져져 백그라운드에서 순차적으로(`await`) 동작하며, 그 사이에도 API 요청(`/admin/...` 등)은 정상적으로 응답(200 OK)합니다.
- **순차 실행 (Sequential Execution)**: KIS API Rate Limit과 DB 락을 회피하기 위해 반드시 `마스터 종목 세팅 -> 일봉 500일 적재 -> 분봉 적재(예정) -> 수급 적재(예정)` 순으로 완전 종료 후 다음 단계로 넘어갑니다.

---

## 3. Global Rate-Limiting Queue Worker

KIS API는 초당 20건 이상의 API 호출 발생 시 IP 차단(`EGW00201` 에러)이라는 치명적인 제재를 가합니다. 이를 원천 봉쇄하기 위해 애플리케이션 레벨의 전역 비동기 큐(`asyncio.Queue`)를 운용합니다.

### 워커(Worker) 설계 원칙
- **절대적 딜레이 (150ms)**: 큐에서 작업을 꺼내어 HTTP 요청을 보내기 전, 반드시 `await asyncio.sleep(0.1)`를 실행합니다. 이를 통해 초당 호출 횟수를 안전하게 약 10회로 강제합니다.
- **Pure Retry Queue 메커니즘**: 만약 네트워크 지연이나 간헐적 API 오류(Timeout 등)가 발생하면, 그 자리에서 Thread를 멈추고(sleep) 재시도하는 대신, 실패한 태스크를 **큐의 맨 마지막으로 다시 던져 넣는(Re-queue) 동적 백오프 방식**을 사용합니다. 이로 인해 단일 워커가 병목에 걸려 전체 파이프라인이 멈추는 현상을 방지합니다.
- **VIP 우선순위 큐 (Priority Queue)**: 큐는 단순 FIFO가 아닌 `asyncio.PriorityQueue`로 구현됩니다. 스케줄러가 대량으로 밀어넣는 수집 작업은 기본 우선순위(7)를 가지며, 관리자가 수동으로 즉각 검증을 요구하는 API(`/admin/.../verify`) 호출은 VIP 우선순위(2)를 가집니다. 따라서 수만 개의 수집 작업이 대기 중이더라도 사용자의 요청은 큐의 맨 앞으로 새치기하여 0.1초 만에 응답됩니다.

---

## 4. 레이턴시 감수 전략 (Trade-off: 6-Minute Latency)

### 현상 및 수학적 계산
Zero-Latency 아키텍처를 달성하기 위해, 우리는 핫리스트 종목뿐만 아니라 2,400여 개의 **전 종목을 전수 조사**하는 단순하고 무식한(Brute-force) 배치 구조를 채택했습니다.
- 2,400종목 × 0.15초 (Rate Limit) = **360초 (정확히 6분)**

### 전략적 수용 (Why we accept this)
6분의 레이턴시(한 바퀴를 도는 데 걸리는 시간)는 실시간 단타 트레이딩에서는 치명적일 수 있습니다. 하지만 우리는 다음과 같은 이유로 이 레이턴시를 긍정적인 '노이즈 필터(Noise Filter)'로 취급하고 적극적으로 수용합니다:

1. **시스템 단순성과 안정성**: 6분을 줄이기 위해 다중 계정 분산, 복잡한 실시간 웹소켓(WebSocket) 동기화, 동적 핫리스트 갱신 로직을 추가하면 1GB RAM 환경에서는 반드시 시스템이 붕괴(OOM)됩니다.
2. **돌파 매매(Breakout) 전략의 본질**: 진정한 추세가 터진 종목(Breakout)이라면 6분 늦게 진입하더라도 하루 종일 상승하는 힘(Momentum)을 유지합니다. 반대로 6분 만에 죽어버리는 종목은 어차피 우리가 피해야 할 '가짜 돌파(Whipsaw)'이므로, 6분의 레이턴시 자체가 훌륭한 필터링 역할을 수행합니다.
3. **유니버스 누락 방지**: 속도를 위해 종목 수를 500개로 억지로 줄였다가, 그날 장을 주도하는 최고의 주도주를 놓치는 기회비용이 6분의 지연보다 훨씬 더 크고 뼈아픕니다.

---

## 5. OOM(Out of Memory) 방지 및 성능 최적화
- **스마트 조기 종료 (Smart Early Termination)**: 매번 500일 치를 맹목적으로 가져오는 것을 막기 위해, 수집 시작 전 DB에서 각 종목별 `MAX(date)`를 미리 해시맵(Dict)에 매핑해둡니다. API 응답의 과거 날짜가 이 `MAX(date)`와 겹치는 순간 더 이상의 과거 추적 루프(`for`문)를 즉시 `break` 처리하여 불필요한 API 호출을 0으로 만듭니다.
- **Daily 워커 (100개)**: 일봉 탐색은 한 번에 100일 치를 가져오며 비동기 I/O 대기시간이 깁니다. 따라서 100개의 워커 Task를 띄워 점진적으로(Max 5회 루프) 과거를 탐색합니다.
- **Minute 워커 (50개)**: 분봉 탐색은 반환되는 JSON 데이터 크기가 일봉에 비해 훨씬 무겁습니다. 메모리 폭발을 막기 위해 워커 수를 절반(50개)으로 줄여 파이프라인을 구동합니다.
