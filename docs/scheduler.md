# 워커 및 스케줄러 아키텍처 상세 명세서 (Scheduler & Worker Specs)

본 문서는 Oracle Cloud Free Tier (1 OCPU, 1GB RAM) 환경의 극단적인 리소스 제약을 극복하고, KIS OpenAPI의 엄격한 호출 제한(Rate Limit)을 우회하기 위한 백엔드 작업 파이프라인의 핵심 구조를 설명합니다.

---

## 1. SystemScheduler (중앙 통제 스케줄러)

메모리 오버헤드와 이벤트 루프 충돌을 막기 위해 모든 예약된 작업(Cron Jobs)은 단일 `SystemScheduler` (Singleton 패턴 기반 `AsyncIOScheduler`) 하에서 중앙 통제됩니다.

### 구동 주기 및 타임라인 (Timeline)
- **`08:30` (마스터 갱신 & Disk Sync)**: 장 시작 30분 전, 거래 정지나 단기 과열에 진입한 위험 종목을 걷어내기 위해 `stock_codes` 테이블을 전면 초기화(`init_stock_codes_db`) 합니다. 완료 직후 **메모리 DB를 물리 디스크에 동기화(Sync)**하여 안전하게 백업합니다.
- **`08:55` (분봉 수집기 기동)**: 장 시작 직전 분봉 워커들을 기동하여 15:55까지 무한 루프로 수집합니다. (장중에는 절대 Disk I/O를 발생시키지 않아 Zero-Latency를 유지합니다)
- **`16:00` (일봉 정규 업데이트 & Disk Sync)**: 장이 완전히 마감된 후, 전 종목에 대해 당일 일봉(`daily_ohlcv`) 데이터를 다운로드하여 적재합니다. (수급 데이터 `daily_investors`는 추후 연동 예정). 완료 직후 **메모리 DB를 물리 디스크에 동기화(Sync)**합니다.
- **`22:00` (인증 토큰 갱신)**: 다음 날 원활한 장중 API 통신을 위해 KIS Auth Token을 갱신합니다. (서버 부팅 시에도 1회 즉시 실행되며, `.env`의 `DEBUG` 값에 따라 운영 환경(`False`)에서는 무조건 강제 재발급(`force=True`), 개발 환경(`True`)에서는 캐시를 재사용(`force=False`)하도록 동적 제어됩니다. 참고: `ADR-007`).
- **`23:00` (가비지 컬렉터 & Disk Sync)**: 매일 밤 11시에 트레이딩 관점에서 무의미해진 과거 데이터(일봉 300일 이전, 분봉 7일 이전)를 일괄 삭제(Time-based Bulk GC)하여 스토리지 낭비를 막고 속도를 최적화합니다. 완료 직후 **메모리 DB를 물리 디스크에 최종 동기화(Sync)**합니다.

---

## 2. 서버 부트스트랩 파이프라인 (Bootstrap Pipeline)

서버 최초 구동 시 DB가 텅 비어있거나, 장기간 서버가 꺼져 있어 대규모 데이터 적재가 필요할 때 FastAPI 응답을 블로킹하지 않도록 설계된 백그라운드 초기화 자동화 시스템입니다.
- **비동기 Non-blocking**: `main.py`의 `lifespan` 훅 안에서 `asyncio.create_task`로 던져져 백그라운드에서 순차적으로(`await`) 동작하며, 그 사이에도 API 요청(`/admin/...` 등)은 정상적으로 응답(200 OK)합니다. 단, 개발 시 `DEBUG=True` 환경 변수가 켜져 있으면 부트스트랩과 스케줄러가 모두 스킵되고 KIS 인증만 강제 1회 수행하여 충돌을 막습니다.
- **순차 실행 (Sequential Execution)**: KIS API Rate Limit과 DB 락을 회피하기 위해 `마스터 종목 세팅 -> 일봉 적재(KOSPI, KOSDAQ 시장별 순차 실행) -> 단일 통합 분봉 스케줄러(Unified Minute Scheduler) 기동` 순으로 완전 종료 후 다음 단계로 넘어갑니다. 특히 일봉 적재는 메모리 보호를 위해 KOSPI와 KOSDAQ을 분리하여 한 시장이 완전히 끝나면 다음 시장을 처리합니다.

### 2.1 일봉 스케줄러의 이원화된 실행 프로세스 (Dual Execution Process)
일봉 스케줄러는 실행 시점의 DB 상태(`daily_ohlcv` 테이블의 적재량)에 따라 두 가지 완전히 다른 행동 양식을 보입니다.

1. **초기 구축 모드 (Empty DB)**
   - **조건**: `daily_ohlcv` 데이터가 비어있을 때 (시장별 100건 미만).
   - **동작**: DB에 각 종목별 최신 날짜(`MAX(date)`) 정보가 없으므로 스마트 조기 종료(Smart Early Termination)가 발동하지 않습니다. 따라서 각 종목당 최대 5회의 API 루프를 꽉 채워 실행하여 500영업일 치의 과거 데이터를 전수 수집(Deep Fetching)합니다.
2. **일일 업데이트 모드 (Populated DB)**
   - **조건**: 이미 충분한 과거 데이터가 DB에 존재할 때 (매일 16:00 정규 스케줄러 구동 시 등).
   - **동작**: 시작 전 종목별 `MAX(date)`를 메모리에 해시맵으로 매핑해둡니다. API 1회 호출 시 반환된 과거 날짜 리스트 중 DB의 `MAX(date)`와 겹치는 순간 즉시 `break` 처리됩니다. 종목당 단 1회의 호출만으로 비어있는 최신 며칠간의 캔들만 빠르게 적재(Shallow Fetching)하여 API 자원 낭비를 0으로 만듭니다.

### 2.2 단일 통합 분봉 스케줄러 (Unified Overlap-Aware Scheduler)
과거에는 과거를 복구하는 백필(Backfill) 태스크와 실시간 폴링 태스크가 분리되어 있었으나, 현재는 모든 상황을 오직 하나의 통합 로직(Self-Healing 메커니즘)으로 커버합니다. (참고: `ADR-010`)
- **부팅 시간 무관**: 서버가 야간이나 주말에 기동되더라도 조건 검사 없이 단일 스케줄러(`start_minute_scheduler_task`)를 즉시 기동합니다.
- **야간 1사이클 완주 보장**: 15:55 장 마감 이후 즉각 셧다운되던 락(Lock)을 루프 최하단으로 이동시켜, 야간에 서버를 켜더라도 큐에 쌓인 전 종목을 무조건 1사이클 완주(과거 빈 데이터 완벽 복구)한 뒤에야 안전하게 종료되도록 설계했습니다.

---

## 3. Global Rate-Limiting Queue Worker

KIS API는 초당 20건 이상의 API 호출 발생 시 IP 차단(`EGW00201` 에러)이라는 치명적인 제재를 가합니다. 이를 원천 봉쇄하기 위해 애플리케이션 레벨의 전역 비동기 큐(`asyncio.Queue`)를 운용합니다.

### 워커(Worker) 설계 원칙
- **절대적 딜레이 (100ms)**: 큐에서 작업을 꺼내어 HTTP 요청을 보내기 전, 반드시 `await asyncio.sleep(0.1)`를 실행합니다. 이를 통해 초당 호출 횟수를 안전하게 약 10회로 강제합니다.
- **Pure Retry Queue 메커니즘**: 만약 네트워크 지연이나 간헐적 API 오류(Timeout 등)가 발생하면, 그 자리에서 Thread를 멈추고(sleep) 재시도하는 대신, 실패한 태스크를 **큐의 맨 마지막으로 다시 던져 넣는(Re-queue) 동적 백오프 방식**을 사용합니다. 이로 인해 단일 워커가 병목에 걸려 전체 파이프라인이 멈추는 현상을 방지합니다.
- **VIP 우선순위 큐 (Priority Queue)**: 큐는 단순 FIFO가 아닌 `asyncio.PriorityQueue`로 구현됩니다. 스케줄러가 대량으로 밀어넣는 수집 작업은 기본 우선순위(7)를 가지며, 관리자가 수동으로 즉각 검증을 요구하는 API(`/admin/.../verify`) 호출은 VIP 우선순위(2)를 가집니다. 따라서 수만 개의 수집 작업이 대기 중이더라도 사용자의 요청은 큐의 맨 앞으로 새치기하여 0.1초 만에 응답됩니다.

---

## 4. 레이턴시 감수 전략 (Trade-off: 4-Minute Latency)

### 현상 및 수학적 계산
Zero-Latency 아키텍처를 달성하기 위해, 우리는 핫리스트 종목뿐만 아니라 2,400여 개의 **전 종목을 전수 조사**하는 단순하고 무식한(Brute-force) 배치 구조를 채택했습니다.
- 2,400종목 × 0.1초 (Rate Limit) = **240초 (약 4분)**

### 전략적 수용 (Why we accept this)
4분의 레이턴시(한 바퀴를 도는 데 걸리는 시간)는 실시간 단타 트레이딩에서는 치명적일 수 있습니다. 하지만 우리는 다음과 같은 이유로 이 레이턴시를 긍정적인 '노이즈 필터(Noise Filter)'로 취급하고 적극적으로 수용합니다:

1. **시스템 단순성과 안정성**: 4분을 줄이기 위해 다중 계정 분산, 복잡한 실시간 웹소켓(WebSocket) 동기화, 동적 핫리스트 갱신 로직을 추가하면 1GB RAM 환경에서는 반드시 시스템이 붕괴(OOM)됩니다.
2. **돌파 매매(Breakout) 전략의 본질**: 진정한 추세가 터진 종목(Breakout)이라면 4분 늦게 진입하더라도 하루 종일 상승하는 힘(Momentum)을 유지합니다. 반대로 4분 만에 죽어버리는 종목은 어차피 우리가 피해야 할 '가짜 돌파(Whipsaw)'이므로, 4분의 레이턴시 자체가 훌륭한 필터링 역할을 수행합니다.
3. **유니버스 누락 방지**: 속도를 위해 종목 수를 500개로 억지로 줄였다가, 그날 장을 주도하는 최고의 주도주를 놓치는 기회비용이 4분의 지연보다 훨씬 더 크고 뼈아픕니다.

---

## 5. OOM(Out of Memory) 방지 및 성능 최적화
- **스마트 조기 종료 (Smart Early Termination)**: 매번 과거 데이터를 맹목적으로 가져오는 것을 막기 위해, 수집 시작 전 DB에서 각 종목별 `MAX(date)`를 미리 해시맵(Dict)에 매핑해둡니다. API 응답의 과거 날짜가 이 `MAX(date)`와 겹치는 순간 더 이상의 과거 추적 루프(`for`문)를 즉시 `break` 처리하여 불필요한 API 호출을 0으로 만듭니다.
- **Daily 워커 (100개) 및 캘린더 일수 타겟팅**: 일봉 탐색은 주말, 연휴, 장기 거래정지를 감안하여 한 번의 API 호출 시 **200 캘린더 일수**만큼 넉넉하게 구간을 설정합니다(KIS API는 이 구간 내 최근 영업일 최대 100건 반환). 100개의 워커 Task를 띄워 Max 5회 루프를 돌아 최대 500영업일 치를 안전하게 수집합니다.
- **시장 분리 스케줄링**: 전체 2,400여 개 종목을 큐에 한 번에 담지 않고, `KOSPI` 작업이 완전히 종료된 후 `KOSDAQ` 작업을 큐에 담아 실행함으로써 Queue 사이즈와 메모리 점유를 절반으로 줄입니다.
- **Minute 워커 (50개) & Zero-Overhead**: 
  - 분봉 탐색 시 당일 분봉 30개만 제공하는 API(`FHKST03010200`) 대신 120개 과거 조회가 가능한 API(`FHKST03010230`)를 사용합니다.
  - 최초 실행 시 `MAX(date || time)`를 활용한 단 1번의 쿼리로 전체 종목의 Gap Analysis를 수행합니다.
  - 최신 분봉 데이터를 수집하면 메모리의 `last_datetime`을 실시간 갱신하여, 다음 루프에서는 단 1번의 API 호출만으로 최신 데이터만 빼오고 조기 종료(Break) 하도록 설계되었습니다.
- **초단순 가비지 컬렉션 (Time-based Bulk GC)**:
  - 종목별 캔들 개수를 강제로 맞추던 기존 분할 삭제(For-Loop Chunking) 방식은 매우 느리고 소외주의 데이터를 방치하는 부작용이 있었습니다. 이를 타파하고 **일봉 300일(약 1년), 분봉 7일(1주일)** 경과 데이터는 무조건 삭제하는 단 2줄의 `DELETE` 쿼리로 대체했습니다.
  - 야간(23:00)에 실행되므로 1 OCPU/1GB 환경에서도 잠깐의 DB Lock은 문제가 되지 않으며, 10분 이상 걸리던 GC 작업이 0.1초 만에 종료되는 압도적인 속도와 용량 최적화를 달성했습니다. (참고: `ADR-015`)

---

## 6. 데이터 적재 및 스케줄링 정합성 룰

### 6.1. 마스터 종목 (`stock_codes`) 필터링 정책 (저장 전 배제 조건)
- 파생상품(ETF/ETN), 리츠, SPAC, 우선주 배제
- 관리종목, 거래정지, 정리매매 종목 배제
- **단기과열종목, 저유동성종목, (코스닥)투자주의환기종목** 완벽 배제
- DB 및 메모리 최적화를 위해 실시간 트레이딩과 직결되지 않는 `prev_vol`(전일거래량), `capital`(자본금) 컬럼은 제외합니다.

### 6.2. 일봉 (`daily_ohlcv`) 데이터 적재 및 정합성 룰
- **수정주가(Adjusted Price)**: KIS API 요청 시 `FID_ORG_ADJ_PRC` 플래그를 활성화하여 액면분할/유상증자 등이 반영된 수정주가를 덮어씁니다 (UPSERT/REPLACE 메커니즘 필수).
- **거래대금(`amount`)**: 일봉 차트에서 세력의 개입 여부를 판단하기 위해 거래량(`volume`) 외에 거래대금 데이터가 필수적입니다.

### 6.3. 분봉 (`minute_ohlcv`) 데이터 정합성 룰 (Boundary Bug 방어)
- KIS API의 1회 반환 캔들 한계(120개)를 돌파하기 위한 고도의 롤링 역추적(Backwards Walking) 로직이 적용됩니다.
- **Empty Fallback (야간/휴일 자가 치유)**: 야간이나 주말에 조회 시 당일 데이터가 없어 API가 빈 데이터(`df.empty`)를 반환하면, 종료(`break`)하지 않고 즉시 날짜를 '하루 전'으로 빼고 시간을 `15:30:00`으로 셋팅하여 과거 탐색을 계속 이어갑니다.
- **Overlap-Aware Early Exit**: 연속된 API 호출 시 가져온 과거 캔들의 시간이 DB의 최신 시간(`last_datetime`)과 단 1초라도 겹치면(Overlap), 필요한 갭을 모두 채웠다고 판단하여 즉시 탐색 루프를 중단(`break`)합니다.
- **날짜 횡단(Date Crossing)**: 영업일 경계인 `09:00:00` 캔들에 도달 시, 다음 조회를 위해 날짜 파라미터를 하루(영업일 기준) 빼서 무중단으로 역추적해야 합니다.
- **거래대금 역산 (누적 차분 룰)**:
  - KIS API는 특정 시간 캔들만의 거래대금이 아닌 **'당일 누적 거래대금'**을 반환합니다.
  - 따라서 해당 캔들의 실제 거래대금(`amount`)을 구하려면 `현재 캔들 누적대금 - 이전 캔들 누적대금`의 차분(Diff)을 계산해야 합니다.
  - ⚠️ **예외 (09:00:00 캔들)**: 장 시작 캔들인 `09:00:00`은 차분 계산을 하지 않고, API가 반환한 누적 거래대금 원본 값 자체를 그대로 `amount`로 저장해야 정합성이 100% 보장됩니다.
- **유동성(거래량)에 따른 달력 탐색 속도 편차 (Trade-off)**:
  - KIS API는 날짜 단위로만 데이터를 반환하므로, 전체 스케줄러의 무한 루프 및 API 쿼터 고갈을 방지하고자 종목당 최대 호출 횟수를 15회(Max Loops)로 고정합니다.
  - 이로 인해 1분마다 거래가 활발한 우량주/주도주는 15회 호출 시 '최근 3~4영업일' 분량의 캔들만 적재하지만, 거래가 극도로 적은 소외주/품절주는 1회 호출 만에 하루 치 캔들이 전부 반환되어 즉각 다음 날짜로 점프하게 됩니다.
  - 결과적으로 거래량이 적은 종목일수록 루프가 달력을 더 빠르게 거슬러 올라가 최대 15달력일(Calendar Days) 이전의 데이터까지 듬성듬성 수집되는 현상이 발생합니다.
  - 이는 단기 돌파 매매(Breakout) 전략상 무의미한 소외주에 대한 API 자원 낭비를 차단하기 위해 의도된 타협점입니다.

---

## 7. 테스트 환경 격리 (Test Execution Isolation)

안전한 무결성 검증을 위해, 라이브 스케줄러 로직을 그대로 호출하면서도 운영 DB(`trading.db`)를 절대 오염시키지 않는 런타임 격리 패턴을 사용합니다.

### 7.1 ContextVar를 이용한 동적 DB 라우팅
- 전역 변수나 싱글톤 패턴 대신 Python의 `contextvars.ContextVar`(`test_db_var`)를 활용합니다.
- `connect_sqlite()` 함수 내부에서 `test_db_var.get()`을 먼저 확인하고, 값이 존재하면 운영 DB 대신 테스트용 DB 파일(`test_trading.db`) 경로로 연결을 우회(Routing)합니다.
- HTTP Request나 Task 단위로 완벽하게 컨텍스트가 분리되므로, 런타임에 여러 워커가 동시에 구동되어도 운영 환경과 테스트 환경이 결코 섞이지 않습니다.

### 7.2 Single Cycle 테스트 모드
- 본래 무한 루프(while True)로 동작하는 분봉 스케줄러(`run_minute_ohlcv_scheduler`)에 `single_cycle: bool` 파라미터를 주입받아, 테스트 모드에서는 단 1회의 수집 사이클만 돌고 즉시 루프를 빠져나오도록(Break) 설계되었습니다.
- 이를 통해 백그라운드 태스크로 던지지 않고도 FastAPI의 엔드포인트 내에서 `await`로 묶어 동기적으로 전체 파이프라인(콜드스타트 ➡️ 복구 ➡️ 검증)을 테스트할 수 있습니다.
