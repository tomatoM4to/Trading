# 에이전트 규칙 (Agent Rules)

- **언어 설정**: 항상 한국어로 대답할 것. (Always speak in Korean)
- **응답 태도**: 사용자의 요청에 대해 친절하고 명확하게 답변하며, 코드 작성 및 수정 시 본 규칙 및 프로젝트 아키텍처를 철저히 준수할 것.
- **문서화 원칙 (`AGENTS.md` 관리 규칙)**:
  - 본 `AGENTS.md` 문서는 프로젝트의 핵심 뼈대이자 아키텍처 가이드라인이므로, 코드가 크게 변경되거나 새로운 아키텍처가 도입될 때마다 사용자의 요청에 따라 최신 상태로 동기화해야 한다.
  - 문서 수정 시 기존의 체계적인 목차 구조(개요, DB 스키마, 백엔드 세부 기능 등)를 유지하며, 복잡한 로직(예: 거래대금 역산, 무결성 검증, 데이터 치유 등)은 그 이유와 한계 극복 과정을 상세하고 전문적으로 기록한다.
  - 기존에 합의된 중요한 설계 원칙이나 규칙을 사용자의 명시적인 지시 없이 임의로 축소하거나 삭제하지 않는다.

---

# 프로젝트 스펙 (Project Specifications)

## 1. 개요 (Overview)
- **프로젝트명**: Trading Server
- **목적**: 한국투자증권(KIS) OpenAPI를 활용하여 전 종목의 데이터를 수집하고, **돌파 매매(Breakout) 전략을 기반으로 상승 가능성이 높은 타겟 종목들을 계산 및 추출**하는 코어 시스템 구축
- **멀티 레포지토리 / 멀티 디렉토리 구조 (예정)**:
  - 현재는 백엔드(FastAPI) 프로젝트만 존재하나, 향후 **웹 프론트엔드(Next.js)** 및 **모바일 앱(React Native)** 프로젝트가 별도 디렉토리로 추가될 예정임.
- **운영 환경 (Environment)**: 모의투자(vps)는 배제하며, 실전투자(PROD) 단일 모드로만 동작함.
- **설정 파일 (Configuration)**: `kis_devlp.yaml` (KIS API 앱키, 앱시크릿, 계좌번호 및 통신 엔드포인트 관리)
- **MCP 연동**: `kis-code-assistant-mcp` 디렉토리에 KIS API 관련 코딩 어시스턴트용 확장 도구 포함.

## 2. 데이터베이스 스키마 (Database Schema)
- **기본 아키텍처**: SQLite 사용, 동시성 관리를 위해 WAL(Write-Ahead Logging) 저널 모드 적용. 외부 API 통신 병목을 없애기 위해 내부 DB만 조회하는 'Zero-Latency 아키텍처' 채택.
- **자가 치유 (Self-Healing)**: DB 파일 삭제/손상 시 서버 재기동만으로 스키마 자동 재생성 및 종목/데이터 롤링 복구 지원.

### 2.1. `stock_codes` (기준 마스터 테이블)
- **데이터 정제**: 파생상품(ETF/ETN), 리츠, SPAC, 우선주 및 거래불가 종목(거래정지, 관리종목) 등을 완벽히 제거하여 '순수 매매용 보통주'만 유지.

| 컬럼명 | 데이터/타입 | 설명 |
|---|---|---|
| `ticker` | 문자열 (PK) | 단축코드 (KIS API 주문/조회용) |
| `name` | 문자열 | 종목명 (한글명) |
| `market` | 문자열 | KOSPI / KOSDAQ |
| `prev_vol` | 숫자형 | 전일거래량 |
| `market_cap` | 숫자형 | 전일기준 시가총액 (단위: 억) |
| `total_shares` | 숫자형 | 상장주수 (단위: 천 주) |
| `capital` | 숫자형 | 자본금 |
| `credit_able` | 문자열 | 신용주문 가능 여부 |
| `margin_rate` | 숫자형 | 증거금비율 |

### 2.2. `daily_ohlcv` (1일봉 테이블)
| 컬럼명 | 데이터/타입 | 설명 |
|---|---|---|
| `ticker` | 문자열 (PK) | 단축코드 |
| `date` | 문자열 (PK) | 영업일자 (YYYYMMDD) |
| `open`, `high`, `low`, `close` | 숫자형 | 시가, 고가, 저가, 종가 |
| `volume` | 숫자형 | 누적 거래량 |
| `amount` | 숫자형 | 누적 거래대금 |

### 2.3. `minute_ohlcv` (1분봉 테이블)
| 컬럼명 | 데이터/타입 | 설명 |
|---|---|---|
| `ticker` | 문자열 (PK) | 단축코드 |
| `date` | 문자열 (PK) | 영업일자 (YYYYMMDD) |
| `time` | 문자열 (PK) | 체결시간 (HHMMSS) |
| `open`, `high`, `low`, `close` | 숫자형 | 분봉 시/고/저/종가 |
| `volume` | 숫자형 | 해당 분의 거래량 |
| `amount` | 숫자형 | 해당 분의 거래대금 (누적대금 차분으로 정밀 역산) |

## 3. 백엔드 세부 기능 (Backend Features)

### 3.1. Lifespan (서버 구동 주기 관리)
- **글로벌 로깅**: `core.logging`을 통해 프로젝트 전역에 Rich 기반의 일관된 로거 적용. `setup_logging()` 호출로 하위 모듈 동기화.
- **초기화 및 무결성 확보**: `init_sqlite_connection()`, `init_stock_codes_db()`를 통해 KIS 공식 마스터 파일을 파싱하고 갱신.
- **API 단일 큐 워커 구동**: KIS API 호출 유량 제한 방어를 위한 큐 실행 (`start_q_worker()`, `stop_q_worker()`). 2026.04 KIS 공식 가이드에 맞춰 0.15초 간격(150ms)을 절대 보장함으로써 `EGW00201` 차단을 방어.
- **자동 인증 토큰 관리**: 기동 시 파일(`KISYYYYMMDD`) 내 캐시 토큰 유효성 검사 및 누락 시 즉시 발급 수행.

### 3.2. Router (API 라우터)
- **통신 객체 래핑**: API 응답은 `APIResp` 클래스 및 내부 `DotDict`를 통해 JSON을 속성으로 직관적 접근. 각 API의 엔드포인트와 `TR_ID`는 전역 스키마 의존도를 낮추기 위해 개별 라우터 함수 내에 명시.
- **관리자 검증 라우터 (`app/routes/admin.py`)**:
  - `daily`와 `minute` 전용 서브 라우터를 분리하여 스케줄러 수동 제어 제공.
  - `/admin/{daily|minute}/check`: 기준 마스터 테이블(`stock_codes`)과 조인하여, 특정 시장(KOSPI/KOSDAQ) 전 종목이 누락 없이 최신 날짜로 100% 갱신되었는지 검증. 일봉은 과거 400일 이상 데이터의 보유율(90% 이상)도 듀얼로 검증.
  - `/admin/{daily|minute}/verify`: 실시간 무작위 표본(Spot Check)을 추출하여 KIS API 실시간 응답과 내부 DB에 적재된 과거/현재 데이터를 1:1로 비교 검증(무결성 100% 증명).

### 3.3. Scheduler (백그라운드 스케줄러)
- **인증 토큰 갱신 (`APScheduler`)**: 매일 밤 10시(22:00)에 새로운 OAuth 토큰을 재발급하여 캐싱.
- **시세 적재 공통 아키텍처 (비동기 큐 & 워커 패턴)**:
  - 전 종목(약 2,500여 개)을 `asyncio.Queue`에 담고, 100개의 비동기 워커(Worker)가 큐에서 종목을 꺼내어 병렬로 처리하는 고성능 구조 채택.
  - **Pandas/Numpy 충돌 방지**: 순수 `dict` 캐스팅 후 DataFrame 변환 적용.
  - **고속 덮어쓰기 (UPSERT)**: 중복 수집 방어 및 수정주가 최신화를 위해 `INSERT OR REPLACE` 기반 벌크 인서트 적용.
  - **Pure Retry Queue 전략**: 내부 재시도(루프+sleep) 로직을 버리고, 타임아웃/유량제한 등의 API 에러 발생 시 0.1초 대기 후 해당 종목을 비동기 큐의 맨 뒤로 보내어 순환 재시도(최대 5회)함으로써 특정 종목 병목 현상을 완벽히 방어.

#### 3.3.1. Daily Scheduler (일봉 적재 상세 아키텍처)
이 모듈은 시장의 전체 트렌드와 장기 지표 계산의 근간이 되는 일봉 과거 데이터를 안정적으로 확보하는 핵심 파이프라인입니다.

- **점진적 과거 탐색 (Backwards Walking)**:
  - KIS API의 일봉 단일 호출 한계인 100영업일을 극복하기 위해, 종목당 최대 5회 루프(약 500영업일 = 2년 치)를 거슬러 올라가며 연속 호출합니다.
  - 매 호출 시 반환된 캔들의 가장 오래된 날짜를 다음 호출의 `end_date`로 설정하여 타임라인을 빈틈없이 이어 붙입니다.
- **수정주가(Adjusted Price) 자동 반영**:
  - `FID_ORG_ADJ_PRC` 플래그를 활용하여 항상 최신의 수정주가를 반영합니다.
  - 매일 스케줄러가 과거 구간을 덮어쓰기(UPSERT) 하므로, 액면분할이나 유무상증자가 발생하여 과거 주가 차트가 변동되더라도 별도의 보정 로직 없이 DB 전체가 자동으로 자가 치유(Self-Healing)되는 효과를 갖습니다.

#### 3.3.2. Minute Scheduler (분봉 적재 상세 아키텍처)
데이트레이딩 및 단기 모멘텀 전략을 위한 핵심 데이터로, KIS API의 극악한 한계점들을 고도의 수학적, 논리적 트릭으로 타파한 모듈입니다.

- **롤링 윈도우 기반 정밀 역추적 (Rolling Window Top-Down)**:
  - KIS 1분봉 API는 한 번에 120개의 캔들만 반환합니다. 이를 위해 현재 시간부터 역순으로 120개씩 쪼개어 수집합니다.
  - **오버랩(Overlap) 로직**: 네트워크 유실이나 캔들 누락을 방지하기 위해 120개 중 끝부분 20분가량을 겹치게(Overlap) 다음 타겟 시간으로 설정하여 촘촘하게 메꾸며 적재합니다. (약 3.5일 치 데이터 초기 확보)
- **분봉 거래대금(Amount) 정밀 역산 로직**:
  - API 자체에서 '해당 1분의 거래대금'을 제공하지 않아, '누적 거래대금(`acml_tr_pbmn`)' 값의 차분(Difference)으로 해당 분의 거래대금을 정밀하게 역산(`df['amount'] = df['cum_amount'].diff()`)합니다.
- **날짜 경계선 치명적 오류(Boundary Bug) 완벽 방어**:
  - 120개의 캔들 뭉치가 '오늘 아침 09:00'과 '어제 오후 15:30'에 걸쳐서 반환될 경우, 아침의 아주 작은 누적대금에서 전날 하루 종일 쌓인 거대한 누적대금을 빼게 되어 **거래대금이 음수(-)가 되는 치명적 데이터 오염**이 발생합니다.
  - 이를 방어하기 위해 Pandas의 `shift()`와 `where()` 조건문을 적용해 **"일자(Date)가 동일할 때만 차분을 인정"**하고, 날짜가 바뀌는 경계선(매일 아침 09:00 캔들 등)에서는 임시방편으로 `종가(Close) × 거래량(Volume)` 공식을 적용하여 데이터를 안전하게 보정하는 방어 코드가 견고하게 구축되어 있습니다.

## 4. 기술 스택 (Tech Stack)
- **언어**: Python >= 3.12 (백엔드)
- **프레임워크**: FastAPI
- **주요 라이브러리**: `apscheduler`, `requests`, `pandas`, `uvicorn` 등
- **버전 관리 및 의존성**: `uv` (`pyproject.toml`, `uv.lock`)

## 5. 실행 방법 (Run & Debug)
- **백엔드 디버그 모드 실행**: 아래 명령어를 통해 핫 리로드 활성화.
  ```bash
  uv run fastapi dev app/main.py
  ```
